{
  "name": "patitas-server",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.ts",
  "license": "ISC",
  "scripts": {
    "start": "ts-node-dev --respawn $npm_package_main",
    "build": "rm -rf ./build && mkdir ./build && tsc --outDir ./build",
    "test": "mocha --exit -t 5000 -r ts-node/register test/**/*.test.ts"
  },
  "dependencies": {
    "body-parser": "^1.19.0",
    "express": "^4.17.1",
    "express-async-router": "^0.1.15",
    "mongodb": "^3.4.1",
    "typescript": "^3.7.4"
  },
  "devDependencies": {
    "@types/chai": "^4.2.7",
    "@types/mocha": "^5.2.7",
    "@types/mongodb": "^3.3.14",
    "axios": "^0.19.1",
    "chai": "^4.2.0",
    "mocha": "^7.0.0",
    "ts-node-dev": "^1.0.0-pre.44",
    "tslint": "^5.20.1"
  }
}